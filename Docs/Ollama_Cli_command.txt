🦙 OLLAMA CLI – COMPLETE PROFESSIONAL CHEAT SHEET

============================================================
SECTION 1: BASIC TERMINAL COMMANDS
==================================

ollama --version
→ Check installed Ollama version

ollama ls
→ List installed models

ollama pull <modelname>
→ Download (pull) a model

ollama run <modelname>
→ Run a model in interactive mode

ollama run <modelname> "your prompt"
→ Run model with single prompt and auto-exit

ollama rm <modelname>
→ Remove model from local system

ollama serve
→ Start Ollama server

ollama signin
→ Sign in to Ollama account

ollama signout
→ Sign out of Ollama account

============================================================
SECTION 2: EMBEDDING MODEL
==========================

ollama run embeddinggemma "Hello world"
→ Generate embedding using embedding model

============================================================
SECTION 3: MULTI-LINE INPUT
===========================

Use triple quotes to write multi-line prompt:

"""
Your multi-line
prompt here
"""

Model will process entire block as one input.

============================================================
SECTION 4: INTERACTIVE SESSION COMMANDS
(Use after: ollama run <modelname>)
===================================

/bye
→ Exit the model session

/clear
→ Clear current session context/history

/help
→ Show help menu

/?
→ Show help menu

/? shortcuts
→ Show keyboard shortcut help

============================================================
SECTION 5: /SHOW COMMAND
========================

/show
→ Show available show commands

Available subcommands:

/show info
→ Show model details

/show license
→ Show model license

/show modelfile
→ Show Modelfile content

/show parameters
→ Show current model parameters

/show system
→ Show system message

/show template
→ Show prompt template

============================================================
SECTION 6: /SET COMMAND (Modify Current Session)
================================================

/set
→ Modify session settings

Available commands:

/set parameter ...
→ Modify generation parameters

/set system <string>
→ Set system message

/set history
→ Enable history

/set nohistory
→ Disable history

/set wordwrap
→ Enable word wrap

/set nowordwrap
→ Disable word wrap

/set format json
→ Enable JSON mode

/set noformat
→ Disable formatting

/set verbose
→ Show LLM stats

/set quiet
→ Disable LLM stats

/set think
→ Enable thinking mode

/set nothink
→ Disable thinking mode

============================================================
SECTION 7: /SET PARAMETER OPTIONS
=================================

/set parameter seed <int>
→ Random seed

/set parameter num_predict <int>
→ Max tokens to predict

/set parameter top_k <int>
→ Select from top-K tokens

/set parameter top_p <float>
→ Select tokens based on probability sum

/set parameter min_p <float>
→ Probability filtering threshold

/set parameter num_ctx <int>
→ Set context window size

/set parameter temperature <float>
→ Set creativity level

/set parameter repeat_penalty <float>
→ Penalize repeated tokens

/set parameter repeat_last_n <int>
→ Repetition window size

/set parameter num_gpu <int>
→ Number of layers sent to GPU

/set parameter stop <string> <string> ...
→ Define stop tokens

============================================================
SECTION 8: SESSION MANAGEMENT
=============================

/load <model>
→ Load a session or model

/save <model>
→ Save current session

============================================================
SECTION 9: HELP MENU (REFERENCE)
/help Output
============

Available Commands:
/set            Set session variables
/show           Show model information
/load <model>   Load a session or model
/save <model>   Save your current session
/clear          Clear session context
/bye            Exit
/?, /help       Help for a command
/? shortcuts    Help for keyboard shortcuts

Use """ to begin a multi-line message.

---

..
